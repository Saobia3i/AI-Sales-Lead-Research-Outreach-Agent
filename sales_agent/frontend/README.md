# Sales Agent Frontend

Next.js + TypeScript + Tailwind UI for the MVP pipeline.

```bash
cd sales_agent/frontend
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the FastAPI backend is not running on `http://localhost:8000`.
